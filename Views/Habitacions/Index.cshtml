@model IEnumerable<Primeruso.Models.dbModels.Habitacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumHabitacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostoHabitacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdHotelNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoHabitacionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumHabitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostoHabitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdHotelNavigation.IdHotel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoHabitacionNavigation.IdTipoHabitacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdHabitacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdHabitacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdHabitacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
